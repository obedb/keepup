<div class="jumbotron">
  <div class="container">
  <div class="row">
    <div class="col-md-6">

     <h4> New Post </h4>      

     <%= form_for :post , url: posts_new_path do |f|%>

       <form>
         <div class="form-group">
            <label for="focusedInput">Title</label>
            <%= f.text_field :title, type:"text", class: "form_control", id: "focusedInput", placeholder: "Title", require:"required" %>
          </div>

          <div class="form-group">
            <%= f.label :Description, class: "focusedInput" %>
            <%=f.text_area :description, class: "form-control", id:"focusedInput", placeholder:"Description...", required:"required"%>
          </div>
<!--           <div class="checkbox">
          <b>Select Category</b>
          <% Category.all.each do |category| %>
            <div class="checkbox">
              <label>
                <%= check_box_tag "post[category_tokens][]", category.id, type:"checkbox", required: "required"%><%= category.name%>
              </label>
            </div>
          <% end %>
          </div> -->
          <div>
            <%= f.label :category_ids, "Slect Post Category" %><br>
            <%= f.collection_select :category_tokens, Category.order(:name), :id, :name, {}, {multiple: true, class: "chosen-select", id:"post_category_tokens"} %>
          </div>
          <div class="checkbox">
            <%= f.submit class: "btn btn-primary active" %>
          </div>
        </form>

     <% end %>
    </div>
  </div>
  </div>
</div>


